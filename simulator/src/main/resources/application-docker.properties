# Overrides for running inside docker-compose — resolves container hostnames
simulator.feeds[0].target-host=mdh-euronext
simulator.feeds[0].file=/app/data/euronext_feed.csv

simulator.feeds[1].target-host=mdh-ebs
simulator.feeds[1].file=/app/data/ebs_feed.csv

simulator.feeds[2].target-host=mdh-fenics
simulator.feeds[2].file=/app/data/fenics_feed.csv